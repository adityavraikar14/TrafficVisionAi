# Local dev: leave unset — Vite's dev-server proxy (vite.config.ts) forwards
# /api and /storage to http://127.0.0.1:8000 automatically.
#
# Production (Vercel): set this to your deployed backend's URL, e.g. a
# Hugging Face Space — https://yourname-trafficvision.hf.space
VITE_API_BASE_URL=
